export type PostsTitle = {
    titulo: string;
    authorName: string;
    publicacao: string;
};
export type PostsNews = {
    id: number;
    categoria_id: number;
    titulo: string;
    data_publicacao: string;
    slog: string;
    url: string;
}